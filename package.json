{
  "name": "better-backend",
  "private": true,
  "scripts": {
    "db": "cd ./pocketbase && ./pocketbase serve",
    "db:reset": "rm -rf ./pocketbase/pb_data && cd ./pocketbase && ./pocketbase serve"
  }
}
